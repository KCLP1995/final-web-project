<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title><%= title %></title>

    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/index.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <!-- Add custom styles -->
    <style>
        /* Adjust space for the MyApp brand/logo */
        .navbar-brand {
            margin-left: 30px; /* Add some left margin */
        }

        /* Ensure the navbar is properly spaced */
        .navbar {
            padding-left: 20px; /* Add padding to the left side */
            padding-right: 20px; /* Add padding to the right side */
        }

        /* Add space between navbar items */
        .navbar-nav .nav-item {
            margin-left: 15px;
        }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">MyApp</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
            <li class="nav-item">
                <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/purchases">Purchases</a>
            </li>

            <!-- Add Login and Register links if user is not logged in -->
            <% if (!user) { %>
                <li class="nav-item">
                    <a class="btn btn-outline-primary ms-2 nav-link" href="/user/login">
                        <i class="bi bi-box-arrow-in-right"></i> Login
                    </a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-success ms-2 nav-link" href="/user/register">
                        <i class="bi bi-person-plus"></i> Register
                    </a>
                </li>
            <% } else { %>
                <!-- If user is logged in, show profile link -->
                <li class="nav-item">
                    <a class="nav-link text-success" href="/user/profile">
                        Welcome, <strong><%= user.username %></strong>
                    </a>
                </li>
            <% } %>
        </ul>
    </div>
</nav>
